# Squelch filter for Caddy bad bots and scanners
[Definition]
failregex = .*"remote_ip":"<HOST>".*"uri":".*/(?:admin|wp-admin|phpmyadmin|xmlrpc\.php|wp-login\.php).*".*
            .*"remote_ip":"<HOST>".*"uri":".*\.(?:php|asp|jsp|cgi).*".*"status":404.*
            .*"remote_ip":"<HOST>".*"status":400.*
            .*"remote_ip":"<HOST>".*"uri":".*(?:\.env|config\.php|wp-config\.php).*".*

ignoreregex =